PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=facefit
# Comma-separated origins. Use * only during local development.
CORS_ORIGIN=*
JWT_SECRET=replace_this_with_a_long_random_secret
